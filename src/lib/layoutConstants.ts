/**
 * Fixed site `Header` uses an inner bar of `h-16` (4rem). Any `<main>` that starts
 * immediately below it needs this top padding or content sits under the navbar.
 */
export const SITE_HEADER_OFFSET = "pt-16";
